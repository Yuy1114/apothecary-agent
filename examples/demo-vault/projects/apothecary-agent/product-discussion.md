# Product Discussion

v0.1 should be a read-only vault reviewer. It should scan the vault, create a knowledge map, and generate maintenance review findings.

It should not move, delete, rename, or rewrite user notes.
